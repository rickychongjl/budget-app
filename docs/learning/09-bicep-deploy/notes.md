# Sitting 9: Bicep and deploy

**Goal:** how a push to `main` ends up live on tightarse.app.
**Files:** `infra/main.bicep`, `infra/modules`, `.github/workflows/deploy.yml`
**Do:** read a `what-if` output.

## Notes

_Written when the sitting is done._
