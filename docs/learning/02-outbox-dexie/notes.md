# Sitting 2: Outbox and Dexie

**Goal:** why a saved transaction is queued in the browser first, and how the queue reaches the server.
**Files:** `src/web/src/features/transactions/useTransactions.ts`, `src/web/src/offline/outbox.ts`, `db.ts`, `OutboxSync.tsx`
**Do:** go offline in DevTools, add a transaction, watch it in Application > IndexedDB, go back online.

## Notes

_Written when the sitting is done._
