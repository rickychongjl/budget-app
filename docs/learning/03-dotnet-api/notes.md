# Sitting 3: .NET API

**Goal:** follow `POST /api/sync` from the endpoint to SQL Server; minimal APIs, dependency injection, EF Core, and why there are four projects.
**Files:** `src/Budget.Api/Endpoints`, `src/Budget.Application/Transactions/Sync.cs`, `src/Budget.Domain`, `src/Budget.Infrastructure/Repositories`

## Notes

_Written when the sitting is done._
