# Copy to .env (gitignored). SQL Server needs 8+ chars with upper, lower, digit and symbol.
MSSQL_SA_PASSWORD=

# Optional: comma-separated Entra object ids allowed to sign in. migrate creates a User row for each.
AUTH_ALLOWED_OIDS=
