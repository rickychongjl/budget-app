namespace Budget.Domain;

// The rows of the editability table in the solution design, section 3.2.
public enum CycleEdit
{
    Categories,
    StartDate,
    OpeningBalance,
    ClosingBalance,
    Transactions,
}
